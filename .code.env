#---------BASIC KEY----------#

Run MiniLM-L6-v2 for Basic/Medium evaluation

PaddleOCR for image → text




Medium/Hard Level AI (3B model):

Run Llama 3.1-3B in CPU mode → slower, but works

Can use LoRA fine-tuning to reduce memory



Optional GPU Upgrade Later:

For Llama 3.1-8B → move to GPU VPS (16–32GB VRAM)